/**
 * 定义标签类型
 * @interface TagType
 * @property {string} name - 标签名称
 * @property {number} value - 标签值
 * @property {string} type - 标签类型
 */
export type TagType = {
  // 标签名称
  name: string;
  // 标签值
  value: number;
  // 标签类型
  type: string;
};
